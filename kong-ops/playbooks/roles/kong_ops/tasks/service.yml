# -------------------------------
# Kong Service Operations (CLI)
# -------------------------------

- name: Start Kong
  command: /usr/local/bin/kong start
  become: yes
  args:
    executable: /bin/bash
  when: kong_action == 'kong_start'
  tags: kong_start

- name: Stop Kong
  command: /usr/local/bin/kong stop
  become: yes
  args:
    executable: /bin/bash
  when: kong_action == 'kong_stop'
  tags: kong_stop

- name: Restart Kong
  command: /usr/local/bin/kong restart
  become: yes
  args:
    executable: /bin/bash
  when: kong_action == 'kong_restart'
  tags: kong_restart
