- name: Start Kong
  service:
    name: "{{ kong_service }}"
    state: started
  when: kong_action == 'kong_start'

- name: Stop Kong
  service:
    name: "{{ kong_service }}"
    state: stopped
  when: kong_action == 'kong_stop'

- name: Restart Kong
  service:
    name: "{{ kong_service }}"
    state: restarted
  when: kong_action == 'kong_restart'
